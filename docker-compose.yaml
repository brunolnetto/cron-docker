version: "3.8"

services:
  cron:
    build: 
      context: .
      dockerfile: Dockerfile
    networks:
      - cron_net
    restart: unless-stopped

networks:
  cron_net:

volumes:
  postgres_data: