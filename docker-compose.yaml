version: "3.8"

services:
  cron:
    build: 
      context: .
      dockerfile: Dockerfile
    networks:
      - CNPJ_net
    restart: unless-stopped

networks:
  CNPJ_net:

volumes:
  postgres_data: